using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Attacking_Monica
{
    public class Monica_DontRemember : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
        // if health goes below 30 will run to nearest healing
        // if health is between 100-70 will stand still and launch ranged attacks?
        // if health is beiween 69 and 35 
    }
}
