using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class M_Error_Code : MonoBehaviour
{
    //FilePathAttribute(“FileName”, FilePathAttribute.Location.ProjectFolder;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
